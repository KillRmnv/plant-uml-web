{
  "name": "plant-uml-web",
  "version": "1.0.0",
  "description": "Plant UML Editor with py-sc-client, py-sc-kpm,sc-web,js-scs-editor",
  "main": "src/backend/app.py",
  "scripts": {
    "build:scg": "cd external/sc-web && npm install && npm run build",
    "start:dev": "python -m http.server 3000 --directory src/frontend",
    "start:proxy": "PYTHONPATH=. python server/proxy_server.py",
    "start:backend": "python src/backend/app.py",
    "install:all": "npm install && cd external/sc-web && npm install"
  },
  "dependencies": {
    "d3": "^7.0.0",
    "jquery": "^3.6.0"
  },
  "devDependencies": {
    "grunt": "^1.6.1",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-watch": "^1.1.0"
  }
}
