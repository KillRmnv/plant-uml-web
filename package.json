{
  "name": "plant-uml-web",
  "version": "1.0.0",
  "description": "Plant UML Editor with py-sc-client, py-sc-kpm,sc-web,js-scs-editor",
  "main": "src/backend/app.py",
  "scripts": {
    "build:sc-web": "npm install && cd external/sc-web && npm install && npm run build",
    "start:frontend": "python -m http.server 3000 --directory src/frontend",
    "start:proxy": "PYTHONPATH=. python server/proxy_server.py",
    "start:backend": "python src/backend/app.py",
<<<<<<< HEAD
    "start:sc-web": "python external/sc-web/server/app.py",
=======
    "start:sc-web":"python external/sc-web/server/app.py",
    "build:backend-linux ": "python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt",
    "build:backend-win": "python3 -m venv .venv && .venv\\Scripts\\activate && pip install -r requirements.txt",
>>>>>>> a1e644d6f4298f211d4226fb81fab9cb5e0645c8
    "install:all-linux": "python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt && npm install && cd external/sc-web && npm install && npm run build",
    "install:all-win": "python3 -m venv .venv && .venv\\\\Scripts\\\\activate && pip install -r requirements.txt && npm install && cd external/sc-web && npm install && npm run build"
  },
  "dependencies": {
    "d3": "^7.0.0",
    "jquery": "^3.6.0"
  },
  "devDependencies": {
    "grunt": "^1.6.1",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-watch": "^1.1.0"
  }
}
