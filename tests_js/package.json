{
  "name": "plant-uml-web-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend tests for plant-uml-web (vanilla JS, jsdom + Vitest)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^24.0.0",
    "vitest": "^1.6.0"
  }
}
